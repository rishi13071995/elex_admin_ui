import { Routes } from '@angular/router';
import { ParentcategoryComponent } from './parentcategory.component';

export const PARENTCATEGORY_ROUTES: Routes = [
  { path: '', component: ParentcategoryComponent },
];
